# Верхняя граница по мажорной версии: aiogram ломает совместимость между 2 и 3,
# и обновление venv не должно однажды уронить службу.
aiogram>=3.7,<4
# SOCKS5 для aiogram: без него схема socks5:// в прокси не поддерживается.
# Нужен, только когда серверы Telegram с хоста недоступны напрямую.
aiohttp-socks>=0.9,<1
